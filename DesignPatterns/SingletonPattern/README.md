# Singleton Pattern

Ensures a class has only one instance, and provides a global point of access to it.
In `Dbc.java`, we implemented a double-checked locking mechanism with a `volatile` instance field to ensure thread safety during initialization.
