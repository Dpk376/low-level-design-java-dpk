# Factory Design Pattern

Defines an interface for creating an object, but let subclasses decide which class to instantiate.

Implemented `DatabaseFactory` to create related sets of query and transaction objects without specifying their concrete classes.
