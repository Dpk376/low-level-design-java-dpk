# Decorator and Flyweight Pattern

- **Decorator Pattern**: Attaches additional responsibilities to an object dynamically. Implemented using IceCream (`ScoopDecorator`, `SyrupDecorator`).
- **Flyweight Pattern**: Shares objects to support large numbers of fine-grained objects efficiently. Implemented using `Bullet` and `BulletFlyWeight`.
