# Prototype and Registry Pattern

- **Prototype Pattern**: Specifies the kinds of objects to create using a prototypical instance, and creates new objects by copying this prototype. Overrides `clone()` to perform copying.
- **Registry Pattern**: A central repository that stores pre-created prototypes to clone from.
